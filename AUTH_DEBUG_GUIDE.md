# 🔧 Диагностика проблем с авторизацией

## Проблема
Ошибка "failed to fetch" при попытке авторизации.

## Возможные причины

### 1. CORS блокировка
- Supabase блокирует запросы с localhost
- Неправильно настроены разрешенные домены

### 2. Сетевые проблемы
- Firewall блокирует запросы
- Proxy сервер мешает
- DNS проблемы

### 3. Конфигурация Supabase
- Неправильный URL проекта
- Неверный API ключ
- Проект приостановлен/удален

## Диагностика

### Шаг 1: Тест подключения
Откройте в браузере:
```
http://localhost:3000/test-supabase-connection.html
```

Запустите тесты по порядку:
1. **Тестировать подключение** - проверяет доступность Supabase
2. **Тестировать авторизацию** - проверяет работу auth API
3. **Тестировать CORS** - проверяет CORS политики

### Шаг 2: Проверьте консоль браузера
Откройте DevTools (F12) и смотрите на ошибки в консоли.

**Типичные ошибки:**
- `CORS policy` - проблемы с CORS
- `net::ERR_NAME_NOT_RESOLVED` - DNS проблемы
- `net::ERR_CONNECTION_REFUSED` - сервер недоступен

### Шаг 3: Проверьте Network вкладку
В DevTools -> Network смотрите на запросы к Supabase:
- Статус код запроса
- Заголовки ответа
- Время выполнения

## Решения

### Если CORS блокировка:

#### 1. Настройте разрешенные домены в Supabase
1. Зайдите в Supabase Dashboard
2. Settings -> API
3. Site URL: добавьте `http://localhost:3000`
4. Additional URLs: добавьте `http://localhost:3000`

#### 2. Проверьте RLS (Row Level Security)
1. В Supabase Dashboard -> Authentication -> Settings
2. Убедитесь что "Enable email confirmations" отключено для тестирования
3. Проверьте RLS политики в Database -> Tables

### Если сетевые проблемы:

#### 1. Проверьте firewall
```bash
# Windows
netsh advfirewall show allprofiles

# Временно отключите firewall для теста
```

#### 2. Проверьте proxy
```bash
# Проверьте переменные окружения
echo $HTTP_PROXY
echo $HTTPS_PROXY
```

#### 3. Попробуйте другую сеть
- Мобильный интернет
- VPN
- Другой WiFi

### Если проблемы с конфигурацией:

#### 1. Проверьте URL проекта
Зайдите в Supabase Dashboard и сверьте URL:
```
https://twjzzmmacmldhfqyyhqi.supabase.co
```

#### 2. Проверьте API ключ
В Settings -> API сверьте anon/public ключ.

#### 3. Проверьте статус проекта
Убедитесь что проект активен и не приостановлен.

## Альтернативные решения

### 1. Используйте CORS прокси
Включите галочку "Обход CORS блокировки" в формах тестирования.

### 2. Локальная разработка без авторизации
Временно отключите проверку авторизации в middleware.ts (уже отключено).

### 3. Используйте другой домен
Вместо localhost попробуйте:
- `127.0.0.1:3000`
- `0.0.0.0:3000`
- Настройте локальный домен

## Проверочный список

- [ ] Supabase проект активен
- [ ] URL и API ключ правильные
- [ ] Localhost добавлен в разрешенные домены
- [ ] CORS заголовки настроены
- [ ] Firewall не блокирует запросы
- [ ] Нет proxy серверов
- [ ] Консоль браузера не показывает CORS ошибки

---

**Используйте тестовую страницу для точной диагностики! 🔍**