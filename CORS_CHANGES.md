# Изменения: Обход CORS блокировки

## Дата: 2026-01-16

## Что добавлено

### 1. Новые файлы

#### `lib/cors-proxy.ts`
Утилиты для работы с CORS прокси:
- `CORS_PROXIES` - список доступных прокси-сервисов
- `getCurrentProxy()` - получить текущий прокси
- `setCurrentProxy()` - установить прокси
- `getCorsProxyEnabled()` - проверить, включен ли обход
- `setCorsProxyEnabled()` - включить/выключить обход
- `applyProxy()` - применить прокси к URL
- `removeProxy()` - удалить прокси из URL

#### `components/CorsProxySettings.tsx`
Компонент для настройки CORS прокси:
- Выбор из 3 доступных прокси-сервисов
- Сохранение выбора в localStorage
- Компактный UI с возможностью скрытия

#### `test-cors-bypass.html`
Интерактивная страница для тестирования:
- Тесты с JSONPlaceholder API
- Тесты с GitHub API
- Тесты с Random User API
- Переключение между прямым запросом и прокси

#### `docs/CORS_BYPASS.md`
Подробная документация на английском

#### `docs/CORS_BYPASS_RU.md`
Краткая инструкция на русском

### 2. Обновленные компоненты

#### `components/ApiTestForm.tsx`
- ✅ Добавлена галочка "Обход CORS блокировки"
- ✅ Интеграция с CorsProxySettings
- ✅ Автоматическое применение прокси к запросам
- ✅ Сохранение настроек в localStorage

#### `components/ApiTestFormLocal.tsx`
- ✅ Добавлена галочка "Обход CORS блокировки"
- ✅ Интеграция с CorsProxySettings
- ✅ Автоматическое применение прокси к запросам
- ✅ Сохранение настроек в localStorage

#### `components/UnifiedApiTester.tsx`
- ✅ Добавлена галочка в ручном режиме
- ✅ Добавлена галочка для AI тестов
- ✅ Интеграция с CorsProxySettings
- ✅ Автоматическое применение прокси ко всем запросам

## Как использовать

1. Откройте любую форму тестирования API
2. Найдите галочку "Обход CORS блокировки"
3. Включите её
4. (Опционально) Нажмите "⚙️ Настроить CORS прокси" для выбора сервиса
5. Выполните запрос как обычно

## Доступные прокси-сервисы

1. **https://corsproxy.io/?** (по умолчанию)
   - Быстрый и надёжный
   - Рекомендуется для большинства случаев

2. **https://api.allorigins.win/raw?url=**
   - Альтернативный вариант
   - Хорошая стабильность

3. **https://cors-anywhere.herokuapp.com/**
   - Классический вариант
   - Может требовать активации

## Технические детали

### Принцип работы
```
Браузер → CORS Прокси → API → Прокси → Браузер
```

### Хранение настроек
- `localStorage.corsProxyEnabled` - включен ли обход (true/false)
- `localStorage.corsProxyUrl` - выбранный прокси URL

### Безопасность
⚠️ Не используйте для конфиденциальных данных в production!
Это решение только для разработки и тестирования.

## Тестирование

Откройте `test-cors-bypass.html` в браузере для интерактивного тестирования.

## Совместимость

✅ Все современные браузеры
✅ Next.js 14+
✅ React 18+
✅ TypeScript

## Что НЕ изменилось

- ❌ Не затронута логика сохранения в БД
- ❌ Не изменена структура данных
- ❌ Не затронуты другие компоненты
- ❌ Нет изменений в API endpoints

## Следующие шаги (опционально)

1. Добавить возможность использовать собственный прокси
2. Добавить индикатор использования прокси в результатах
3. Добавить статистику по использованию прокси
4. Добавить автоматическое переключение при ошибке

---

**Все изменения протестированы и готовы к использованию! 🚀**
