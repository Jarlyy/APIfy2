# Обход CORS блокировки

## Описание

Добавлен функционал обхода CORS (Cross-Origin Resource Sharing) блокировки при тестировании API. Это позволяет тестировать внешние API напрямую из браузера без ошибок CORS.

## Как использовать

### 1. Включение обхода CORS

В любой форме тестирования API найдите галочку **"Обход CORS блокировки"** и включите её.

### 2. Выбор прокси-сервиса

После включения обхода CORS появится кнопка **"⚙️ Настроить CORS прокси"**. Нажмите на неё, чтобы выбрать один из доступных прокси-сервисов:

- `https://corsproxy.io/?` - Быстрый и надёжный сервис
- `https://api.allorigins.win/raw?url=` - Альтернативный вариант
- `https://cors-anywhere.herokuapp.com/` - Классический вариант (может требовать активации)

### 3. Тестирование API

После включения обхода CORS все запросы будут автоматически проходить через выбранный прокси-сервис, что позволит обойти ограничения браузера.

## Где доступно

Функционал обхода CORS доступен во всех компонентах тестирования:

- ✅ **ApiTestForm** - основная форма тестирования с сохранением в БД
- ✅ **ApiTestFormLocal** - локальная форма без БД
- ✅ **UnifiedApiTester** - унифицированный тестер с AI генерацией

## Технические детали

### Как это работает

Когда обход CORS включен, ваш запрос:
1. Отправляется не напрямую к целевому API
2. Проходит через прокси-сервис
3. Прокси-сервис делает запрос к API от своего имени
4. Возвращает результат с правильными CORS заголовками

### Сохранение настроек

Настройки обхода CORS сохраняются в `localStorage`:
- `corsProxyEnabled` - включен ли обход (true/false)
- `corsProxyUrl` - выбранный прокси-сервис

### Файлы

- `lib/cors-proxy.ts` - утилиты для работы с CORS прокси
- `components/CorsProxySettings.tsx` - компонент настройки прокси
- Обновлены все формы тестирования API

## Важные замечания

⚠️ **Безопасность**: Не отправляйте конфиденциальные данные (токены, пароли) через публичные прокси-сервисы в продакшене.

💡 **Совет**: Если один прокси не работает, попробуйте другой из списка.

🔧 **Для разработки**: Этот функционал предназначен для тестирования и разработки. В продакшене настройте CORS на стороне сервера.
