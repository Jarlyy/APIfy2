# Развертывание на Vercel

## CORS Прокси на Vercel

### ✅ Что работает

Локальный CORS прокси (`/api/proxy`) **полностью работает на Vercel**:

- ✅ Все HTTP методы (GET, POST, PUT, DELETE, PATCH)
- ✅ Передача заголовков и тела запроса
- ✅ Обход CORS ограничений браузера
- ✅ Поддержка JSON и текстовых ответов
- ✅ Автоматические CORS заголовки

### ⚠️ Ограничения Vercel

1. **Таймаут функций**
   - Hobby план: 10 секунд
   - Pro план: 60 секунд
   - Наш прокси использует таймаут 8 секунд для безопасности

2. **Размер ответа**
   - Hobby план: 6MB
   - Pro план: 50MB

3. **Холодный старт**
   - Первый запрос может быть медленнее (100-500ms)
   - Последующие запросы быстрые

### 🚀 Оптимизации для Vercel

Прокси оптимизирован для Vercel:

```typescript
// Таймаут 8 секунд (меньше лимита Vercel)
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 8000);

// User-Agent для лучшей совместимости
headers['user-agent'] = 'APIfy-Proxy/1.0';

// Отключение кэширования
'Cache-Control': 'no-cache, no-store, must-revalidate'
```

### 📊 Производительность

**Локальная разработка:**
- Время ответа: 50-200ms
- Без ограничений по времени

**Vercel Hobby:**
- Время ответа: 100-500ms (холодный старт)
- Время ответа: 50-200ms (теплый)
- Таймаут: 8 секунд

**Vercel Pro:**
- Время ответа: 50-300ms
- Таймаут: 60 секунд
- Больше ресурсов

### 🔧 Настройка для продакшена

1. **Переменные окружения**
   ```bash
   # В Vercel Dashboard добавьте:
   GEMINI_API_KEY=your_key
   HUGGINGFACE_API_KEY=your_key
   NEXT_PUBLIC_SUPABASE_URL=your_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
   ```

2. **Домен и HTTPS**
   - Vercel автоматически предоставляет HTTPS
   - Можно настроить кастомный домен

3. **Мониторинг**
   - Vercel Analytics для отслеживания производительности
   - Логи функций в Vercel Dashboard

### 🛠️ Альтернативы прокси

Если прокси не подходит, есть альтернативы:

1. **Внешние CORS прокси**
   ```typescript
   // Настройка в CorsProxySettings
   const externalProxies = [
     'https://cors-anywhere.herokuapp.com/',
     'https://api.allorigins.win/raw?url=',
   ];
   ```

2. **Браузерные расширения**
   - CORS Unblock
   - CORS Toggle

3. **Прямые запросы**
   - Для API с поддержкой CORS
   - Публичные API без ограничений

### 📈 Мониторинг и отладка

**Логи Vercel:**
```bash
# Просмотр логов
vercel logs your-deployment-url

# Реальное время
vercel logs your-deployment-url --follow
```

**Отладка прокси:**
```typescript
// В браузере
console.log('Proxy request:', {
  url: targetUrl,
  method,
  headers,
  body
});
```

### 🎯 Рекомендации

1. **Для большинства API** - используйте локальный прокси
2. **Для медленных API** - увеличьте таймаут или используйте внешний прокси
3. **Для больших ответов** - рассмотрите Pro план Vercel
4. **Для критичных приложений** - настройте мониторинг

## Заключение

CORS прокси отлично работает на Vercel и подходит для большинства случаев использования. Ограничения Vercel редко влияют на тестирование API, а производительность остается высокой.